<<: !include esp32-ble-jk04-example-debug.yaml

jk_bms_ble:
  - ble_client_id: client0
    update_interval: 1s
    throttle: 0s
    id: bms0
    enable_fake_traffic: true
    protocol_version: ${protocol_version}
